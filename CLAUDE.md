# CLAUDE.md — Microbial Clinical Atlas (MCA)

## Project overview

MCA is a curated clinical knowledge base for the human microbiome. It organizes microbial information into standardized **Taxon Passports** — structured records capturing taxonomic identity, ecological context, clinical associations, and evidence-linked references (PMIDs). The goal is to support reproducible interpretation of microbiome sequencing data in clinical and translational research.

**Maintained by:** Bioinformatics Hub
**Current version:** v1.5
**License:** MIT

---

## Tech stack

- **Backend:** PHP 7.4+ with PDO (MySQL)
- **Database:** MySQL (InnoDB, utf8mb4), 10-table schema centered on `passport`
- **Frontend:** Vanilla HTML/CSS/JS (no frameworks); Google Fonts (Montserrat, Roboto)
- **Deployment:** LAMP stack; web root is `/web`
- **Export:** Python (`database/xml2sql.py`) converts versioned XML → MySQL INSERT dumps

---

## Directory layout

```
MCA/
├── web/                        # Main web application
│   ├── index.php               # Homepage with live search autocomplete
│   ├── passport.php            # Individual taxon passport detail view
│   ├── passports.php           # Browse/table view of all taxa
│   ├── ajax_search.php         # AJAX autocomplete endpoint
│   ├── about.php               # Database stats and curation overview
│   ├── glossary.php            # Controlled vocabulary glossary
│   ├── header.php / footer.php # Shared page templates
│   ├── db_connect.php          # Credentials (gitignored — do not commit)
│   ├── db_connect_template.php # Copy this to db_connect.php and fill in credentials
│   ├── css/style.css           # Main stylesheet
│   └── images/                 # Logo and favicon
├── database/
│   ├── MCA_create_database.sql     # Full schema definition (10 tables)
│   ├── MCA_DB_v1_5_20260402.xml    # Current versioned XML snapshot
│   ├── MCA_DB_v1_5_20260402.sql    # Current SQL dump (generated by xml2sql.py)
│   ├── MCA_schema.xsd              # XML schema definition
│   ├── xml2sql.py                  # Converts XML snapshot → MySQL INSERT dump
│   └── curation_log.json           # Audit log of all applied staging files
├── staging/                    # Staging JSON files from mca-paper-curator (gitignored)
│   ├── YYYY-MM-DD_taxon-name.json  # Pending (not yet applied)
│   └── applied/                    # Applied files (audit trail — never deleted)
└── .claude/
    └── skills/
        ├── mca-paper-curator/  # Skill 1: paper → staging JSON (8-agent, 4-phase pipeline)
        └── mca-xml-update/     # Skill 2: staging JSON → XML + SQL + web/data copy
```

---

## Primary entities in the MCA knowledge base

MCA is fundamentally a KB about **linking taxa to clinical and biological features** through structured, evidence-backed records. The primary entities and their relationships are:

### 1. Taxon (core entity)
A curated microbial organism — bacterium, fungus, virus, or archaeon — at any taxonomic rank (family, genus, species, strain). Each taxon has exactly one **Taxon Passport**.

- Identified by a stable `passport_id` (format: `MCA-[DOMAIN]-[NNNNNN]`, e.g. `MCA-BAC-000001`)
- Linked to NCBI Taxonomy via `ncbi_taxid`
- Can have multiple **Synonyms** (historical names, aliases)

### 2. Taxon Passport (record entity)
The central structured record for a taxon. A passport is versioned and reviewed over time. It organizes all knowledge about a taxon into four feature groups:

**Biology** — intrinsic microbiological properties:
- Gram status, oxygen tolerance, morphology
- Key traits (e.g., spore-forming, biofilm-producing)

**Ecology** — environmental and host context:
- Primary niches (body sites or environments)
- Reservoirs (human, animal, environment)
- Transmission routes (how the organism is acquired)

**Clinical profile** — clinically relevant features:
- Pathobiont status (`yes` / `no` / `context dependent` / `unknown`)
- Clinical roles (e.g., opportunistic pathogen, protective commensal)
- Typical specimen types (e.g., blood, stool, respiratory)
- Bloom triggers (conditions enabling overgrowth, e.g., antibiotic exposure)
- Risk contexts (vulnerable populations or settings, e.g., ICU, immunosuppressed)
- AMR highlights (notable resistance phenotypes, e.g., ESBL, CRE)

**Evidence** — general PMID citations supporting the passport as a whole

### 3. Clinical Association (claim entity)
An individual, evidence-graded claim linking a taxon to a specific clinical condition or outcome (e.g., "associated with increased risk of CDI recurrence"). Each association is:
- Linked to exactly one Taxon Passport via `passport_id`
- Graded by evidence level:
  - **E3** — Strong human clinical evidence (guidelines, meta-analyses, systematic reviews, or multiple independent human cohorts within a single paper)
  - **E2** — Moderate human evidence (single cohort, single RCT, case-control, or cross-sectional study)
  - **E1** — Limited / preliminary (animal models, in vitro studies, case reports, mechanistic work)
- Supported by one or more PMIDs

### 4. Evidence / PMID (support entity)
A PubMed citation (PMID) that backs a specific claim. PMIDs attach at two levels:
- **Taxon-level**: general references supporting the overall passport (`mca_taxon_evidence_pmid`)
- **Claim-level**: specific references for each clinical association (`mca_clinical_association_pmid`)

### Entity relationship summary

```
Taxon (1) ──── (1) Taxon Passport
                    │
                    ├── (N) Biology / Ecology / Clinical feature lists
                    ├── (N) Synonyms
                    ├── (N) Taxon-level PMIDs
                    └── (N) Clinical Associations
                                │
                                └── (N) Claim-level PMIDs
```

The KB's core purpose is to make the links between **taxa**, **clinical features**, and **supporting evidence** explicit, controlled, and reproducible across studies.

---

## Database schema overview

The schema uses 10 tables. `passport` is the central entity.

**Core tables:**
- `passport` — one row per taxon; surrogate `INT` PK + stable `passport_id VARCHAR(20)` (e.g. `MCA-BAC-000001`), `preferred_name`, `taxon_rank`, `domain`, `lineage`, `ncbi_taxid`, `is_pathobiont`, `last_reviewed`, `created_at`, `updated_at`
- `biology` — gram status, oxygen tolerance, morphology, BacDive URL (1:1 with passport)
- `taxon_tag` — all list-type passport attributes in one table; `category` ENUM distinguishes type: `synonym`, `key_trait`, `primary_niche`, `reservoir`, `transmission_route`, `role`, `typical_specimen`, `bloom_trigger`, `risk_context`, `amr_highlight`; `ext_id VARCHAR(20)` holds MeSH/KEGG/ARO IDs
- `metabolite` — metabolite relationships (produces/consumes/modifies) with KEGG Compound and ChEBI IDs
- `passport_pmid` — taxon-level PMID citations

**Evidence tables:**
- `association` — individual evidence-graded clinical claims linked to `passport.id`; `content_hash CHAR(64)` for dedup
- `assoc_pmid` — PMIDs per association (many-to-many)
- `assoc_ref` — ontology references per association; `ref_type ENUM('mesh','kegg_disease')`, `ref_id`, `ref_label`

**System tables:**
- `meta` — DB version string (e.g. `v1_0_20260401`), stats
- `migrations` — schema version tracking for iOS delta sync

**Evidence grading:** E1 (limited/preliminary) → E2 (moderate) → E3 (strong clinical) — UNCERTAIN associations are stored in XML but excluded from the SQL dump by xml2sql.py

---

## Local setup

1. Import schema: `mysql < database/MCA_create_database.sql`
2. Import data: `mysql MCA < database/MCA_DB_v1_5_20260402.sql`
3. Configure DB credentials: `cp web/db_connect_template.php web/db_connect.php` then edit (host=localhost, user=mca, db=MCA)
4. Point web server root to `/web`
5. To regenerate the SQL dump from XML: `python3 database/xml2sql.py database/MCA_DB_v1_5_20260402.xml`

---

## Key conventions

- **Never commit `web/db_connect.php`** — it contains credentials and is gitignored.
- `passport_id` values are stable identifiers; do not reassign or recycle them.
- All SQL queries use PDO prepared statements — maintain this pattern to prevent SQL injection.
- The canonical data source is `database/MCA_DB_v*.xml`; the `.sql` file is derived from it via `xml2sql.py`. Re-run xml2sql.py after any XML change.
- Evidence claims must be accompanied by at least one PMID. UNCERTAIN associations are stored in XML but excluded from the SQL dump.
- Controlled vocabulary terms (roles, niches, gram status, etc.) must match `.claude/skills/mca-paper-curator/references/CONTROLLED_VOCABULARY.md`.
- Applied staging files live in `staging/applied/` — never delete them (audit trail).

---

## UI color system

All colors are defined inline in PHP templates or in per-page `<style>` blocks. Do not introduce new semantic colors without updating this table.

### Evidence grades (used in `passport.php` and `passports.php`)

| Grade | Background | Text | Border | Class |
|-------|-----------|------|--------|-------|
| E3 — Strong | `#dcfce7` | `#166534` | `#4ade80` | `.eg-E3` / `.ev-E3` |
| E2 — Moderate | `#fef3c7` / `#fef9c3` | `#92400e` / `#854d0e` | `#fbbf24` | `.eg-E2` / `.ev-E2` |
| E1 — Limited | `#f3f4f6` | `#6b7280` | `#d1d5db` | `.eg-E1` / `.ev-E1` |

### Pathobiont status (used in `passport.php` and `passports.php`)

| Value | Background | Text | Class |
|-------|-----------|------|-------|
| `yes` | `#007bff` | `#fff` | `.pb-active-yes` / `.pc-pathobiont-yes` |
| any other | `#f3f4f6` | `#6b7280` | `.pb-active-other` / `.pc-pathobiont-other` |

### Ontology reference badges (used in `passport.php` — Clinical Associations)

| Type | Background | Text | Border | Class |
|------|-----------|------|--------|-------|
| MeSH | `#d1fae5` | `#065f46` | `#6ee7b7` | `.assoc-ref-mesh` |
| KEGG | `#ffedd5` | `#9a3412` | `#fdba74` | `.assoc-ref-kegg` |
| Other | `#f5f5f5` | `#555` | `#ddd` | `.assoc-ref-other` |

MeSH/KEGG section labels (`MeSH:` / `KEGG:`) are rendered at 13px bold `#333` (neutral, no color).

### Inline ext_id badges (used in `passport.php` — Clinical Profile fields)

| Type | Background | Text | Border | Used on |
|------|-----------|------|--------|---------|
| ARO (CARD) | `#fee2e2` | `#991b1b` | `#fca5a5` | `amr_highlights` — links to `card.mcmaster.ca/ontology/{id}` |
| KEGG Drug | `#ffedd5` | `#9a3412` | `#fdba74` | `bloom_triggers` — links to `kegg.jp/entry/{id}` |
| VFDB | `#fce7f3` | `#9d174d` | `#f9a8d4` | `virulence_factors` — no link (local DB only) |

### Related taxa match tags (used in `passport.php`)

| Tag | Background | Text | Border | Class |
|-----|-----------|------|--------|-------|
| Niche | `#efefef` | `#555` | `#d8d8d8` | `.rt-match-niche` |
| Risk | `#f0ece8` | `#7a6a5f` | `#ddd0c8` | `.rt-match-risk` |

These are intentionally muted to avoid competing with evidence, pathobiont, MeSH, and KEGG colors.

### Clinical roles pill (used in `passports.php` card view)

| Element | Background | Text | Border | Class |
|---------|-----------|------|--------|-------|
| Role pill | `#f0f4ff` | `#404f7c` | `#d0d8f0` | `.pc-role-pill` |

### Metabolite relationship badge (used in `passport.php`)

| Element | Background | Text | Border | Class |
|---------|-----------|------|--------|-------|
| Relationship type | `#eef0f8` | `#404f7c` | `#c8cde8` | `.metabolite-rel` |

### Brand / structural colors

| Use | Value |
|-----|-------|
| Primary brand / accent | `#404f7c` (navy-indigo) |
| Related taxon pill hover border | `#404f7c` |
| Timeline year | `#404f7c` |
| BacDive link | `#404f7c` |
| PubMed links | `#007bff` |
| Page background | `#fff` |
| Card / section backgrounds | `#f8f9fa`, `#fafafa`, `#f5f6fa` |
| Section title / muted text | `#888`, `#aaa`, `#bbb` |
| Dividers / borders | `#eee`, `#e8e8e8`, `#f0f0f0` |

---

## Project aims (for context)

1. **Standardize** microbial knowledge into consistent Taxon Passports with stable identifiers.
2. **Link evidence** explicitly — every clinical claim is tied to PMIDs and an evidence grade.
3. **Support interpretation** of microbiome signals in clinical/translational settings.
4. **Enable reuse** via versioned, structured exports (XML) for downstream tools and web apps.
